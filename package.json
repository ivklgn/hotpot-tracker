{
  "name": "hotpot-tracker-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm --filter client lint",
    "format": "prettier --write .",
    "prepare": "husky",
    "instant:push": "pnpm --filter @hotpot/shared instant:push"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "3.8.0"
  }
}
